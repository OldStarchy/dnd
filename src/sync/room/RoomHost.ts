import type Room from './Room';
import type RoomPublication from './RoomPublication';

export default class RoomHost {
	readonly host: string;
	constructor() {
		// TODO:
	}

	async publish(room: Room): Promise<RoomPublication> {
		// TODO:
	}
}
